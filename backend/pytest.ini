[pytest]
asyncio_mode = auto

# IMPORTANT: match your session-scoped event_loop + session-scoped async fixtures
asyncio_default_fixture_loop_scope = session

testpaths = tests
addopts = -q
filterwarnings =
    ignore::pytest.PytestRemovedIn9Warning
